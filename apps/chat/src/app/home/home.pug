log-in-modal(#logInModal)
.alert.alert-danger(*ngIf="error") {{ error }}
h1 Welcome to FreeDB Chat
div(*ngIf="freedb.client.hosts.primary.user")
  p You're logged in as {{ freedb.client.hosts.primary.user.$.id }}
  form((submit)="startChat()")
    .form-group
      .input-group
        input.form-control([(ngModel)]="chatRoomName", placeholder="Room name (optional)", name="chatRoomName")
        .input-group-append
          button.btn.btn-success(type="submit") Start a new chat
  .row
    .col
      h2 Your Chatrooms
      .chat(*ngFor="let chat of userChats")
        a([routerLink]="['/chat', chat.$.id]") {{ chat.title || chat.$.id }}
    .col
      h2 Latest Chatrooms
      .chat(*ngFor="let chat of publicChats")
        a([routerLink]="['/chat', chat.$.id]") {{ chat.title || chat.$.id }}
div(*ngIf="!freedb.client.hosts.primary.user")
  p Sign in to get started

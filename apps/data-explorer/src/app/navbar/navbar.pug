log-in-modal(#logInModal)
nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
  .container
    a.navbar-brand(routerLink="/") OneDB Data Explorer
    div
      ul.navbar-nav.ml-auto
        li
          a.nav-link((click)="logInModal.open()")
            i.fa.fa-left.fa-users
            span([ngSwitch]="!!onedb.client.hosts.primary.user")
              span(*ngSwitchCase="true")
                span {{onedb.client.hosts.primary.displayName}}
              span(*ngSwitchCase="false") Sign In

